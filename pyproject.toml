[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "telegram-browser-bot"
version = "1.7.1-alpha.1"
description = "A Telegram browser bot with encrypted sessions and runtime access control."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.13,<4",
    "beautifulsoup4>=4.12,<5",
    "cryptography>=44,<46",
    "fastapi>=0.115,<1",
    "httpx>=0.27,<1",
    "pydantic-settings>=2.5,<3",
    "playwright>=1.52,<2",
    "python-dotenv>=1.0,<2",
    "uvicorn[standard]>=0.30,<1",
]

[project.optional-dependencies]
dev = ["pytest>=8.3,<9"]

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]
